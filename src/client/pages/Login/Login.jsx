import React, { useContext } from 'react';
import {} from '@atomikui/core';
import Layout from '../../components/Layout';

const Login = () => {
  return <Layout>Login</Layout>;
};

export default Login;
